#include "krankboard.dtsi" // The shared dtsi file is included in the overlay
#include <dt-bindings/zmk/matrix_transform.h>

&uart0_sleep: uart0_sleep {
        group1 {
            psels = <NRF_PSEL(UART_RX, 0, 8)>,
                <NRF_PSEL(UART_TX, 0, 6)>;
            low-power-enable;
       		};
	};
